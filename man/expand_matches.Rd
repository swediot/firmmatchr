% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/crosswalk.R
\name{expand_matches}
\alias{expand_matches}
\title{Expand Matches to Every Original Dictionary Row}
\usage{
expand_matches(x, crosswalk = dict_crosswalk(x))
}
\arguments{
\item{x}{A result returned by \code{\link[=match_companies]{match_companies()}}.}

\item{crosswalk}{The dictionary crosswalk. Defaults to the one attached to
\code{x} by \code{\link[=match_companies]{match_companies()}}; supply it explicitly if \code{x} has been subset or
round-tripped through another format.}
}
\value{
A \code{data.table} with columns \code{query_id}, \code{query_name}, \code{dict_id},
\code{dict_name}, \code{match_type}, \code{dict_id_rep}, \code{n_dict_ids} and
\code{is_representative}. Unambiguous matches contribute exactly one row.
}
\description{
\code{\link[=match_companies]{match_companies()}} reports one representative dictionary id per match. When
normalization collapsed several dictionary rows onto the same string, this
function expands the result to one row per (query row, original dictionary
row) pair, so the original strings behind a collapsed group are visible.
}
\examples{
queries <- data.frame(
  query_id = 1:2,
  company_name = c("Meier", "BMW")
)
dictionary <- data.frame(
  orbis_id = c("D001", "D002", "D003"),
  company_name = c("Meier GmbH", "Meier AG", "BMW AG")
)

\donttest{
results <- match_companies(queries, dictionary)

# One row for BMW, two rows for the collapsed Meier group.
expand_matches(results)
}
}
\seealso{
\code{\link[=dict_crosswalk]{dict_crosswalk()}}
}
