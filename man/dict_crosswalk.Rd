% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/crosswalk.R
\name{dict_crosswalk}
\alias{dict_crosswalk}
\title{Dictionary Crosswalk of a Match Result}
\usage{
dict_crosswalk(x)
}
\arguments{
\item{x}{A result returned by \code{\link[=match_companies]{match_companies()}}.}
}
\value{
A \code{data.table} with one row per original dictionary row that entered
matching, and columns \code{name_clean} (the normalized string),
\code{dict_id}/\code{dict_name} (the original id and string), \code{dict_id_rep} (the id
reported by \code{\link[=match_companies]{match_companies()}} for that group), \code{n_dict_ids} (group size)
and \code{is_representative}.
}
\description{
Because \code{\link[=normalize_company_name]{normalize_company_name()}} is lossy, several dictionary rows can
collapse onto the same normalized string (e.g. \code{"Meier GmbH"} and
\code{"Meier AG"} both become \code{"meier"}). \code{\link[=match_companies]{match_companies()}} matches against one
representative row per group and attaches this crosswalk so that every
original dictionary row can still be recovered.
}
\examples{
queries <- data.frame(
  query_id = 1:2,
  company_name = c("Meier", "BMW")
)
dictionary <- data.frame(
  orbis_id = c("D001", "D002", "D003"),
  company_name = c("Meier GmbH", "Meier AG", "BMW AG")
)

\donttest{
results <- match_companies(queries, dictionary)

# Both Meier rows survive in the crosswalk, even though only D001 is
# reported as the match.
dict_crosswalk(results)
}
}
\seealso{
\code{\link[=expand_matches]{expand_matches()}}
}
